.button
  border 1px solid rgba(0, 0, 0, 0.1)
  color darkgrey
  font-weight bold
  display flex
  align-items center
  padding 0 5px
  cursor pointer
  transition all 0.7s ease
  &:hover
    background black
    color white
.icon
  padding 0 5px
  font-size: 30px